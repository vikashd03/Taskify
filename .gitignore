# Ignore node_modules in both server and client directories
server/node_modules/
client/node_modules/

# Ignore build or dist directories (if applicable) in both server and client directories
server/build/
server/dist/
client/build/
client/dist/

# Ignore environment files or configuration files (if applicable)
server/.env
client/.env

# Ignore log files
server/*.log

# Ignore IDE and editor files (specific to your editor)
.vscode/
.idea/
